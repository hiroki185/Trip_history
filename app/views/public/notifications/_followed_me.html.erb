<%= link_to transition_path(notification) do %>
  <div style="border-bottom-width: 3px; border-bottom-style: dotted; border-bottom-color: #a9a9a9;">
    <% if notification.subject.follower.guest? %>
      ゲストユーザー
    <% else %>
      <%= link_to notification.subject.follower.name, user_path(notification.subject.follower) %>
    <% end %>
    があなたをフォローしました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>