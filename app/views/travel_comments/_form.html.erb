<%= form_with model: [travel, travel_comment], local: false do |f| %>
  <%= f.text_area :comment, rows:'5', cols:'50', placeholder: "コメントをここに", class: "form-control col", id: "comment_textarea" %>
  <%= f.submit "送信", class: 'btn btn-outline-success rounded-pill col' %>
<% end %>